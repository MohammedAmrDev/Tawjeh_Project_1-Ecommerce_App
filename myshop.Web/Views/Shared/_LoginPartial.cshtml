@if (User.Identity is not null && User.Identity.IsAuthenticated)
{
	@if(User.IsInRole(nameof(UserTypeEnum.Customer)))
	{
		<span class="text-muted" style="margin-right: 0.5rem;">Hello @User.Identity.Name</span>

	}
	else if (User.IsInRole(nameof(UserTypeEnum.Admin)))
	{
		<div class="dropdown">
			<button class="btn btn-primary dropdown-toggle" type="button" id="userDropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
				<i class="fa-solid fa-user" style="margin-right:5px"></i>
				@User.Identity.Name
			</button>
			<div class="dropdown-menu" aria-labelledby="userDropdown">
				<a class="dropdown-item" asp-controller="Product" asp-action="Index">Product Management</a>
				<a class="dropdown-item" asp-controller="Category" asp-action="Index">Category Management</a>
			</div>
		</div>
	}

	<form id="logoutForm" class="d-inline" asp-controller="Account" asp-action="Logout">
		<button id="logout" type="submit" class="btn btn-danger">Logout</button>
	</form>
}
else
{
	<a class="btn btn-outline-primary me-2" id="register" asp-controller="Account" asp-action="Register">Register</a>
	<a class="btn btn-primary" id="login" asp-controller="Account" asp-action="Login">Login</a>
}
